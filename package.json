{
  "name": "avnir-studio-core",
  "private": true,
  "packageManager": "pnpm@9.6.0",
  "engines": { "node": ">=22 <23" },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:tokens": "pnpm -w --filter @avnir/tokens build",
    "build:ladle": "pnpm -w --filter @avnir/ladle build",
    "build:site": "pnpm run build:tokens && pnpm run build:ladle",
    "check:hex": "rg -n \"#[0-9a-fA-F]{3,6}\" packages/ui apps | rg -v \"packages/tokens|/fixtures|/svg\" && echo \"⛔ HEX trouvés\" && exit 1 || echo \"✓ Pas de HEX\""
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.1.3",
    "typescript": "^5.6.3"
  }
}
