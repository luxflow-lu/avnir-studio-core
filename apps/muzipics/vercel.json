{
  "buildCommand": "cd ../.. && pnpm install && pnpm turbo run build --filter=muzipics",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
