{
  "name": "@avnir/tokens",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "files": [
    "dist"
  ],
  "exports": {
    "./tokens.json": "./src/tokens.json"
  },
  "scripts": {
    "build": "echo 'No build step needed - JSON files are used directly'",
    "lint": "echo 'No lint configured'",
    "typecheck": "echo 'No typecheck needed for JSON'",
    "clean": "echo 'No build artifacts to clean'"
  },
  "devDependencies": {
    "rimraf": "^6.0.0"
  }
}
