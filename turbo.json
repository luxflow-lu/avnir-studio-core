{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": [".next/**", "dist/**", "build/**", "lib/**"] },
    "dev": { "cache": false },
    "lint": { "outputs": [] },
    "typecheck": { "outputs": [] }
  }
}
